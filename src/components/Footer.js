import React from 'react';

const Footer = () => {
  return (
    <footer className="bg-brown-footer h-16">
      {/* Footer content can be added here if needed */}
    </footer>
  );
};

export default Footer;
